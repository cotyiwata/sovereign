"""Sovereign Intelligence — shared core modules.

Modules:
  constants     — All policy constants (stop buffers, leverage table, etc.)
  market_data   — yfinance fetch + indicator computation
  llm           — Ollama wrapper with retry + JSON mode
  badges        — Single badge system (replaces 13 duplicate CSS classes)
  enrichment    — Active trade + position watch enrichment, broken into stages
"""
